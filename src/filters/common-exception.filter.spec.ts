import { CommonExceptionFilter } from './common-exception.filter';

describe('CommonExceptionFilter', () => {
  it('should be defined', () => {
    expect(new CommonExceptionFilter()).toBeDefined();
  });
});
